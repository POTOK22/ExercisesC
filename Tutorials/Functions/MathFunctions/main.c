#include <stdio.h>
#include <math.h>

int main(void)
{
    printf("%f\n", sqrt(16));

    // rounding the number
    printf("%f\n", ceil(1.4)); // ceil -> goes up
    printf("%f\n", floor(1.4)); // floor -> goes down

    printf("%f\n", pow(4,3)); // 4^3 = 4*4*4 = 64

    return 0;
}